# fieldnotes
A browser-based statistics notebook for CSV/Excel data — descriptive stats, one-way ANOVA, correlation, Cronbach's alpha, and frequency tables, with charts. Runs entirely client-side, no backend, no data leaves your browser.
